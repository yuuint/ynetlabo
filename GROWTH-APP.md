# アプリ本体の成長ループ（2026-09 〜 2027-01）

[GROWTH.md](GROWTH.md) はサイト（ynetlabo.net）側の運用書。こちらは**アプリの中**の話。

サイトの技術的 SEO は整っている。だが数字を見ると、アプリの新規獲得は
サイトからでもストア検索からでもなく、**ユーザーが貼る招待リンク**から来ている。
その在庫の 7 割を捨てているのが、いまいちばん大きな伸びしろ。

この文書は 2026-09 から 2027-01 までの実行計画。**予算ゼロで組んである。**

---

## 1. 出発点の数字（これが基準線）

出どころは `ynetlabo-analytics`（`reports/latest.md`）。
GA4 は 2026-08-01〜08-26 の 26 日間、RDS 由来は 2026-08-27 時点の累計。

### アプリ別

| 指標 | wa/ri | soroe | tsutsum | mapengu |
|---|---:|---:|---:|---:|
| GA4 アクティブ | **531** | 121 | 21 | — |
| GA4 新規 | 240 | 20 | 13 | — |
| 実利用率（実利用/アクティブ） | 33.0% | 8.3% | — | — |
| 前月比（新規） | +88% | **-59%** | -13% | — |

アクティブの 78.9%、イベントの 80.6% が wa/ri。**Y.NetLabo は実質 wa/ri 一本**。

### wa/ri の内訳（ここに答えがある）

| 指標 | 値 | 意味 |
|---|---:|---|
| 招待が入口（アカウント作成 10 分以内）| **431 / 606 = 71.1%** | 参加者の 7 割は招待リンクから来た |
| ルームが共有されていない | **290 / 413 = 70.2%** | 招待が飛んでいるルームは 3 割だけ |
| 支出 0 件（作っただけ）| **128 / 413 = 31.0%** | 作成直後に離脱している |
| 精算まで到達 | **19 / 413 = 4.6%** | 体験の最後まで行っていない |
| 共有ルーム 1 件あたりの招待経由参加 | 431 / 123 = **3.5 人** | 共有 1 回の価値 |

### 全体

| 指標 | 値 |
|---|---:|
| 累計アカウントのうち「当日のみ」| **2,350 / 2,702 = 87.0%** |
| 1 か月超 使い続けている | 121 = 4.5% |
| 広告収益（月） | 1.94（wa/ri 1.89） |
| ASA 実績（2026-06） | ¥3,657 / 48 install = **CPI ¥76** |

利用のピークは**日曜 21.1% / 土曜 17.3%**、時間帯は **15〜16 時と 21〜22 時**。
飲み会・旅行の直後に開かれている。

---

## 2. 診断

### 共有率がすべての律速になっている

共有ルーム 1 件が 3.5 人を連れてくる。共有率を 29.8% → 60% にすれば、
招待経由の参加は理論上ほぼ倍（431 → 約 860／月）。

CPI ¥76 で換算すると **+400 人/月 = 月 3 万円の広告出稿と同等**。
しかも一度直せば恒久的に効く。記事を何本書くより効率がよい。

### 実装を確認した結果、穴は 3 つ

2026-08-28 に各リポジトリを読んで確認した。

| # | 穴 | 状態 | 影響 |
|---|---|---|---|
| 1 | **招待の共有イベントが GA4 に出てこない** | `sendAnalyticsShareLinkEvent` は**実装済みで、両 Flutter の `editor_room_share_page.dart` から呼ばれている**（GA4 標準の `share`）。にもかかわらず 8 月の wari のイベント一覧に `share` が 1 件も無い | 共有率を GA4 で追えない。実装を足す話ではなく、**出てこない原因の切り分け**から |
| 2 | **精算完了の計測イベントが無い** | 同上 | 4.6% がどう動いたか、翌月まで分からない |
| 3 | ~~Flutter 側にレビュー依頼が無い~~ → **実装済みだった（訂正）** | `wari-android/wari_flutter` に `in_app_review: ^2.0.9` と `review_manager.dart` があり、発火点も iOS と同じ 2 箇所。**最初の調査で `wari-webapp`（Web / LIFF 用）だけを見ていた**のが誤りの原因 | 開発は不要。残るのは効果測定だけ |

**Flutter は 2 リポジトリに分岐している。** `wari-android/wari_flutter/lib` と
`wari-webapp/wari_flutter/lib` は共通パッケージではなくコピーで、既に多数の差分がある
（前者だけに `admob_*` と `review_manager`、後者だけに `liff_service`）。
**Flutter への変更はすべて 2 回入れる必要がある。**

### 支出 0 件 31% は、まだ原因が分かっていない

同じ数字に、少なくとも二通りの読み方がある。どちらかで打ち手がまるごと変わる。

- **(a) 招待でつまずいている** — 呼びたいのに呼び方が分からない、面倒。導線の改善で解ける
- **(b) 幹事がアプリを使うのをやめた** — 試しに作って、そのまま離れた。導線をいくら直しても
  解けない。問題は機能ではなく入口のほうにある

**いまのデータでは区別できない。** 累計アカウントの 87.0% が当日かぎりで離脱している事実は、
むしろ (b) を支持する。9 月 W1 の計測は、これを分けるために入れる。
**それまで、原因を前提にした施策は打たない**（§3 の 3「測れないものは変えない」）。

### Android は「収益源」ではなく「伸びしろ」として見る

wa/ri の Android はアクティブの **11.7%（79 / 673）**、iOS 280 の約 1/3.5。
日本の端末シェアから見れば取りこぼしていて、W4 のレビュー依頼移植はその入口にあたる。
ただし**広告収益の話と混ぜないこと**。理由は 2 つ。

- **プラットフォーム別の広告収益は測っていない。** `ad_revenue` / `ad_by_format` /
  `ad_by_unit` はいずれも `app=` と `adFormat=` のみ、`by_platform` は `activeUsers` と
  `sessions` しか持たない。「iOS と Android のどちらが稼いでいるか」は現状答えられない
- **金額が判断に耐えない。** 月合計は 7 月 4.067 / 8 月 1.944 だが、**7 月は 7/8・7/24・7/29 の
  3 日で 70.4%（2.864）を占める外れ値込み**。この 3 日を除いた 7 月の日平均は 0.0481、
  8 月は 0.0748 で、**平常日で比べると 8 月のほうが 55% 高い**。月合計の増減だけを見ると
  逆の結論になる。この規模では月次の合計を傾向として読まない

Android を伸ばす判断は、収益ではなく**ユーザー数**（`by_platform`）で行う。

### soroe は集客より先に直すところがある

| 指標 | 値 |
|---|---:|
| 募集を作っていないルーム | 7 / 17 = 41.2% |
| 確定まで到達 | 2 / 17 = 11.8% |
| 実利用率 | 8.3% |

来た人の 9 割が使わずに帰っている。**この状態で集客しても全部漏れる。**
9〜10 月は soroe に集客をかけない。

---

## 3. 意思決定のルール

計画に迷ったら、この 4 つで決める。

1. **穴を塞いでから水を入れる。** 共有率・精算到達・soroe のオンボーディングが
   直るまで、集客の施策は増やさない
2. **wa/ri に寄せる。** 5 アプリ横並びが最大の非効率。tsutsum は ASO だけ、
   soroe はオンボーディングだけ、mapengu / hue は広報素材としてのみ扱う
3. **測れないものは変えない。** 改修の前に計測イベントを入れる。
   効果が判定できない改修は、次の判断材料にならない
4. **割り込んで要求しない。** 価値を受け取る前に、他人を巻き込む要求をしない。
   導線は**常設**で置き、必要になった瞬間に目に入るようにする。モーダルと強制遷移は使わない。
   iOS の `ReviewManager` が既にこの原理で書かれている——計画のほうを、そこに揃えた

---

## 4. 90 日計画

### 9 月 — 穴を塞ぐ

| 週 | やること | 終わったと言える条件 |
|---|---|---|
| W1<br>9/1–9/7 | **計測を通す。** まず `share` が GA4 に出てこない原因を切り分ける（実装は既にある）。そのうえで精算完了と、支出 0 件ルームの内訳を足す → §6 R1 | GA4 で共有が観測でき、精算完了と支出 0 件の内訳が日次で引ける |
| W2<br>9/8–9/14 | **空のルーム画面の主導線を「メンバーを招待」にする。** 招待画面への強制遷移やモーダルは出さない。支出 0 件のときに出る空状態そのものを、招待が主アクションのデザインに置き換える。閉じる操作が要らないので、まだ呼ぶ気がない人の邪魔にならない | 空ルームの主アクションが招待になっている。強制遷移とモーダルは無し |
| W3<br>9/15–9/21 | **割り勘結果の画面に「未招待のメンバー」を常設する。** 保存直後のポップアップはやめる（連続入力の邪魔になる）。出す場所は `/room/split`、誰が誰にいくら払うかを見終えて共有したくなる瞬間。iOS の `ReviewManager` が「価値を受け取り終えた満足の瞬間」として既に使っている画面と考え方を揃える | 結果画面に常設表示。入力中に割り込むものは増えていない |
| W4<br>9/22–9/30 | **レビュー依頼は実装済みだった。開発は不要。** やることは**ストアのレビュー件数の現在値を控える**ことだけ → §6 R4。空いた 1 週は R2 / R3 の予備に回す | 両ストアのレビュー件数と平均評価を記録した |

9 月末に `make report` を回して、共有率が動いたかを見る。あわせて W1 の計測で
**支出 0 件 31% の原因**（離脱か、滞在したうえでの未入力か）を判定する。
**原因が分かるまで、割り込みを増やす施策は入れない。**

### 10 月 — 名前を知らせる

ストア審査に 1〜2 週間かかる。**忘年会シーズンに間に合わせるには 10 月中に出す。**

| 週 | やること | 終わったと言える条件 |
|---|---|---|
| W5<br>10/1–10/12 | **tsutsum の ASO を全面的に書き直す。** 「香典 記録」「ご祝儀 記録」「慶弔 記録」は検索需要があって競合が薄い。月 21 アクティブは需要が無いのではなく**見つかっていない**。`aso-store-copy` スキルの手順どおり、禁則 → 軸 → キーワード配分 → 各欄の順で | App Store / Google Play の両方に提出済み |
| W6<br>10/13–10/19 | **wa/ri の ASO に季節語を仕込む。** 「忘年会」「新年会」「幹事」「二次会」「打ち上げ」。App Store はキーワード欄、Google Play は簡単な説明（80 字）が効く | 両ストアに提出済み。11 月頭に反映される見込み |
| W7<br>10/20–10/26 | **アプリ紹介メディアへ掲載依頼。** Appliv / アプリブ / iPhone Mania / アプリオ など。「割り勘アプリ おすすめ」の検索結果を握っているのは彼らで、自分で 1 位を取るより彼らのリストに入るほうが速い。被リンク（GROWTH.md §5 の未対応項目）も同時に取れる | 4 媒体以上に送信済み。送り先と日付を記録 |
| W8<br>10/27–11/2 | **忘年会シーズンの実用記事。** 222〜225 と同じ型（表や計算を先に見せて、解説は後ろ）。「忘年会の幹事がやること」「会費の集め方と釣り銭」あたり | 公開 → `npm run indexnow` |

### 11 月 — 山に備える

「忘年会」の検索は 11 月中旬から立ち上がる。**そこにインデックス済みで待っている状態**を作る。

| 週 | やること |
|---|---|
| W9–W10<br>11/3–11/16 | 季節記事を公開してインデックスさせる。クロス投稿（GROWTH.md §4）で被リンクを足す。X は金曜夕方と土日に出す（利用ピークが日 21% / 土 17% なので、需要の直前に当たる） |
| W11–W12<br>11/17–11/30 | **soroe のオンボーディングを直す。** 募集未作成 41% が的。ルーム作成直後に募集作成へ誘導する。年末年始シフトを組むのは 11 月なので、ここに間に合わせる |

---

## 5. 12 月〜1 月 — 本番

**この期間は改修を入れない。** 障害を出すと最大の需要期を落とす。やるのは運用だけ。

- 週 1 で `make report` を回して、共有率・精算到達・新規を見る
- ストアのレビューに返信する（評価の付き方が変わる）
- 不具合が出たら直す。それ以外は触らない

1 月末に、9 月からの改修が効いたかを判定する。

---

## 6. 見る指標

`ynetlabo-analytics` で取れるものだけを挙げる。取れないものは指標にしない。

| 指標 | 2026-08（基準）| 11 月末 | 1 月末 | 見る場所 |
|---|---:|---:|---:|---|
| ルーム共有率 | 29.8% | 45% | **55%** | `55_share_acquisition` / `60_wari_rooms` |
| 支出 0 件ルーム | 31.0% | 24% | **18%** | `60_wari_rooms` |
| 精算まで到達 | 4.6% | 9% | **15%** | `60_wari_rooms` |
| 招待経由の参加（月）| 431 | 600 | **900** | `55_share_acquisition` |
| tsutsum GA4 アクティブ | 21 | 60 | **120** | `overview` |
| wa/ri Android アクティブ | 79 | 130 | **200** | `by_platform` |
| soroe 確定到達 | 11.8% | 25% | **35%** | `70_soroe_periods` |
| 全体 GA4 アクティブ | 673 | 1,000 | **1,500** | `overview` |

Google Play / App Store のレビュー件数も並べる。**現在値を最初に控えておくこと**
（この文書を書いた時点では未確認）。W4 のレビュー依頼が効いたかは、ここでしか分からない。

### 読み方の注意

- `userEngagementDuration` は使わない。放置セッションの外れ値が混ざる（`latest.md` の注記参照）
- 「当日のみ 87%」は累計値。月次で改善しても動きが鈍いので、四半期で見る
- 共有率は RDS 側の累計。改修の効果を日次で見たいときは W1 で入れた
  `invite_shared` を GA4 で見る
- **広告収益のプラットフォーム別内訳は取れるようになった**（`ad_by_platform`、2026-08-29 追加）。
  ただし **iOS が AdMob 側で未リンクのうちは iOS が 0 のまま**なので、
  リンクするまで内訳は「Android 100%」としか出ない。月次の合計も傾向として読まない
  （7 月は 3 日で 70.4%）。詳細は §5.1

---

## 5.1 マネタイズ（広告収益の推移と見込み）

計画を見直すたびに更新する。**毎回最新を取る必要はない。**

### 月次推移（2026-03 → 08）

| | 3 月 | 4 月 | 5 月 | 6 月 | 7 月 | 8 月 |
|---|---:|---:|---:|---:|---:|---:|
| GA4 アクティブ | 209 | 215 | 532 | 539 | 552 | 673 |
| 広告収益 | — | — | — | — | 4.067 ⚠ | 1.944 |
| 広告表示 | — | — | — | — | 1,381 | 1,577 |

**アクティブは 3 月の 209 から 8 月の 673 へ 3.2 倍**。その間ずっと広告収益はほぼ横ばい。
そもそも **広告データは 2026-07-04 が最古**で、3〜6 月は AdMob 連携前のため DB に存在しない
（`ad_revenue` / `ad_by_format` / `ad_by_unit` はいずれも 07-04 開始。利用側の
`overview` / `by_platform` は 2026-02 から取れる）。8 月は 26 日間の集計。⚠ は外れ値込み。

> **この行の「広告収益」「広告表示」は Android だけの数字**（2026-08-29 確定）。
> iOS は 1 度も計上されていない。単位は **USD**。詳細は下の「iOS の広告が…」。

### 実績（GA4 `ad_revenue`）

| | 7 月 | 8 月 |
|---|---:|---:|
| 月合計 | 4.067 | 1.944 |
| 日数 | 28 | 26 |
| 日平均 | 0.1453 | 0.0748 |
| 表示 | 1,381 | 1,577 |
| eCPM | 2.945 | 1.233 |

**7 月の 70.4%（2.864）は 7/8・7/24・7/29 の 3 日**に集中している。
この 3 日を除いた 7 月の日平均は 0.0481 で、**平常日なら 8 月のほうが 55% 高い**。

### 見込み（30 日換算）

表示数がアクティブ数に比例すると仮定し、現行 eCPM 1.233 で外挿したもの。

| | 7 月 | 8 月 | 11 月末 | 1 月末 |
|---|---:|---:|---:|---:|
| 月間収益 | 4.36 ⚠ | 2.24 | 3.33 | 5.00 |

⚠ は外れ値込み。**アクティブが 2.2 倍になっても月 5 程度**——これが収益化の判断を
2027 年 2 月まで先送りしている理由。

### プラットフォーム別 — 収益比率の裏付け

**2026-08-29 追記: 代理指標は要らなくなった。** GA4 Data API を `platform` 次元つきで
叩けば実数が取れる（下の「iOS の広告が…」）。結果は **Android 100% / iOS 0%** で、
比率の話そのものが成り立たない。以下は経緯として残す。

**広告を積んでいるのはどこか**（コードで確認、2026-08-29）

| クライアント | 広告 SDK | 根拠 |
|---|---|---|
| iOS `wari-ios` | あり | `GoogleMobileAds`（SPM）、`BannerContentView.swift`、`ExportRewardedInterstitialService.swift` |
| Android `wari-android/wari_flutter` | あり | `google_mobile_ads: ^6.0.0`、`admob_config.dart`、`admob_widgets.dart` |
| Web / LIFF `wari-webapp/wari_flutter` | **なし** | AdMob 関連ファイルも依存も存在しない |

**wa/ri のセッション（2026-08）**

| | セッション | 構成比 | 広告在庫 |
|---|---:|---:|---|
| iOS | 432 | 53.1% | あり |
| Web / LIFF | 245 | 30.1% | **なし** |
| Android | 137 | 16.8% | あり |

利用の 30.1% を占める Web は 1 表示も生まない。

### iOS の広告が GA4 に一度も計上されていない（2026-08-29 確定）

**確定した。GA4 に届いている広告データは 100% Android。iOS は 0。**
GA4 Data API を `platform` 次元つきで直接叩いて確認した
（プロパティ 490392808 / **通貨 USD** / TZ Etc/GMT-9）。

**2026-07-01 〜 08-29**

| platform | 収益 (USD) | 表示 | クリック |
|---|---:|---:|---:|
| Android | 6.4687 | 2,162 | 51 |
| **iOS** | **0** | **0** | **0** |
| web | 0 | 0 | 0 |

ユニット別も全部 Android（`ads-banner` 6.3824 / 2,159、`ads-award-beta` 0.0863 / 3）。
**`ads-banner` は Android のユニットだった**（前の版で「どちらか分からない」としていた点）。

**iOS は 2025-05 以降 16 か月、表示が 1 度も 0 を超えていない。**
一方 iOS の Firebase 自体は生きている（同期間で `screen_view` 8,665 / `first_open` 112 /
`tran_created` 903）。**`ad_impression` / `ad_click` / `ad_reward` だけがまるごと欠けている。**

#### 原因は管理画面側。ほぼ特定できている

Android の `ad_impression` は **2026-07-04 に突然始まっている**（07-04 に 5 件、以降継続）。
その直前の Android のコミットは 07-03 の認証画面の修正だけで、広告にも計測にも触れていない。
`google_mobile_ads` の実装と本番のユニット ID は **2026-04-12〜14 から動いており、
4〜6 月は Android も表示 0 だった**。

> コードは 4 月から変わっていないのに、7/4 を境に計上が始まった。
> = **7/3〜4 に AdMob 管理画面で Android アプリを Firebase にリンクした**
> （またはユーザー指標を有効にした）。**iOS には同じ操作がされていない。**

`ad_impression` / `ad_click` / `ad_reward` は**アプリが送っているイベントではない**。
iOS・Android どちらのコードにも `logEvent("ad_impression")` は無い。
Google Mobile Ads SDK が自動で送るもので、**AdMob ↔ Firebase のリンクが有効なときだけ**送られる。
だから片方のプラットフォームだけ 0 になりうる。

#### iOS 側のコードは全部シロ（2026-08-29 確認）

| 確認項目 | 結果 |
|---|---|
| `FirebaseAnalytics` を SPM でリンク | ✅ `project.pbxproj` に productName = FirebaseAnalytics |
| Google Mobile Ads SDK | ✅ 12.14.0（自動計測の要件 7.60.0 以上を満たす）|
| `MobileAds.shared.start()` | ✅ `wariApp.swift:15` |
| `GADApplicationIdentifier` | ✅ `ca-app-pub-…~6932809048`（`Info.plist:31`）|
| `GoogleService-Info.plist` | ✅ `wari-39ca1` / `1:894927958805:ios:280c…` / `ynetlabo.wari` |
| `GADDelayAppMeasurementInit` | ✅ 設定なし（あると自動計測が止まる）|
| ATT（v2.4 / 2026-08-02）| ✅ **無関係**。ATT は IDFA を止めるだけでイベント収集は止めない。そもそも ATT 導入の 15 か月前から 0 |
| `setAnalyticsCollectionEnabled(false)` | ✅ `admin` 権限（＝開発者自身）のときだけ。一般ユーザーは有効 |

→ **アプリの修正は不要。管理画面の設定だけ。**

#### やること（AdMob 管理画面。コードは触らない）

1. AdMob → アプリ → **iOS の「割り勘 wa/ri」** → アプリ設定 → **Firebase にリンク**
   → `wari-39ca1` の `ynetlabo.wari`（`1:894927958805:ios:280c2009a083031b8faf1c`）
2. 同じ画面の **「ユーザー指標」を有効**にする
3. Android のアプリで同じ画面を開いて設定を見比べる（そちらは 7/4 から効いている）

**直ったと言える条件**: GA4 で `platform=iOS` の `publisherAdImpressions` が 0 を超える。
Android の前例では**リンクした翌日から**出ている。AdMob の iOS レポートの「広告視聴者」も 0 でなくなる。

#### 副産物 — リワード関連イベントの内訳（累計 / 2025-01 以降）

| イベント | 誰が送っているか | Android | iOS |
|---|---|---:|---:|
| `ad_impression` | **SDK 自動** | 2,162 | **0** |
| `ad_click` | **SDK 自動** | 51 | **0** |
| `ad_reward` | **SDK 自動** | 50 | **0** |
| `cheer_ad_completed` | アプリ | 26 | 13 |
| `export_pdf_ad_completed` | アプリ（**iOS のみ実装**）| 0 | 17 |
| `export_csv_ad_completed` | アプリ（**iOS のみ実装**）| 0 | 10 |

**SDK 自動イベントは iOS が全部 0、アプリ自前のイベントは iOS にも入っている。**
これが「壊れているのは計上経路だけ」の直接の証拠。
**iOS でもリワードは実際に再生されている**（`cheer_ad_completed` 13 件）。AdMob 側に載っていないだけ。

**ただし、これで全部は説明できない。** Android 内でも
**`ad_reward` 50 に対して AdMob のリワード表示は 3** で合っていない。
これは iOS のリンクとは別の話で、**未解決のまま残す**（着手はしない）。

#### 実数の取り方（`ynetlabo-analytics` 側・対応済み）

`config/config.toml` と `config.example.toml` に次を追加した（2026-08-29）。
GA4 Data API は履歴を返すので、**取り込めば 2026-07-04 まで遡って実数が入る**。

| 追加したレポート | dimensions |
|---|---|
| `ad_by_platform` | `["platform"]` |
| `ad_by_unit_platform` | `["platform", "adUnitName"]` |
| `by_event_platform` | `["eventName", "platform"]` |

`by_event` が `["eventName"]` だけだったせいで「`ad_impression` 1,126 が iOS か Android か
分からない」という状態になっていた。`by_event_platform` はその再発防止。

### フォーマット別の単価（累計）

| フォーマット | 収益 | 表示 | 1 表示あたり |
|---|---:|---:|---:|
| Banner / wa/ri | 5.7475 | 1,907 | 0.0030 |
| Banner / soroe | 0.1773 | 1,048 | 0.0002 |
| Rewarded interstitial / wa/ri | 0.0863 | 3 | **0.0288** |

**「リワードはバナーの 9.6 倍」は取り下げる。** 根拠が AdMob 集計の表示 3 回という
極小サンプルだけだった。アプリ側のイベントは `ad_reward` 50、`cheer_ad_completed` 39、
`export_pdf_ad_completed` 17、`export_csv_ad_completed` 10 で、**リワードは数十回動いている**。
2026-08-29 に**内訳だけは分かった**（上の「副産物」の表）。
`export_pdf_ad_completed` 17 と `export_csv_ad_completed` 10 は **iOS だけの数字**
（Android に実装が無い。iOS も 2026-06-06 までに削除済みで、いま出ているのは旧ビルド）。
`cheer_ad_completed` 39 は Android 26 + iOS 13。
**`ad_reward` 50 が全部 Android なのに AdMob のリワード表示が 3 しかない点は、依然として未解決。**
**リワードの単価はリンク後に取り直す。** いまの 0.0288 は Android の 3 表示だけの数字で、
比較には使えない。いずれにせよ着手はしない——月 2〜5 の収益に開発時間を割く判断にはならない。

### 注意

- **単位は USD で確定**（2026-08-29、GA4 Data API の `metadata.currencyCode`）。
  AdMob 管理画面は円表示なので、**そのまま足さないこと**。
  8 月の Android eCPM は $1.83 で、円と読むと相場から外れる（前の版の誤読の原因）
- クリック数が一桁の月は CTR も単価も参考値にならない。日次で見る
- **広告データは 2026-07-04 より前に存在しない。** GA4 の保持期間の問題ではなく、
  **それ以前は Android も AdMob 連携が入っていなかったから**（2026-08-29 に判明）。
  長期の推移を見るときは利用側の指標と並べること

---

## 6. 改修計画（コメントで着手を決めたもの）

話題ごとに 1 項目。詳細はアーティファクトの同名ページに対応する。

### R1 計測を通す（9 月 W1）

**決定**: 診断の穴 1・2 に「進めましょう」（2026-08-28、コメント）

`share` は実装済みなのに GA4 に出てこない。**実装を足す前に、出てこない原因の切り分けから。**

1. GA4 の DebugView で発火自体を確認する。出ているなら `ynetlabo-analytics` 側の抽出条件、
   出ていないならコードパスへの到達を疑う。**ここが分かるまで先へ進まない**
2. 独自イベント名（`invite_shared`）に寄せるか決める。`share` は GA4 標準名で
   `share_warikan_image` と混ざる可能性がある
3. `room_settled`（精算完了）を追加
4. 支出 0 件を「作成後に離脱」「滞在したが未入力」に分ける
5. ~~**`ad_by_platform` を `config.toml` に足す**~~ → **完了（2026-08-29）**。
   `ad_by_platform` / `ad_by_unit_platform` / `by_event_platform` を
   `config.toml` と `config.example.toml` に追加済み。**次の `make ingest` で
   2026-07-04 まで遡って実数が入る** → §5.1
6. ~~**AdMob の iOS アプリが Firebase にリンクされているか確認する**~~ → **原因確定（2026-08-29）**。
   **GA4 の広告データは 100% Android、iOS は 16 か月ゼロ。** iOS 側のコードに欠陥は無い。
   残っているのは管理画面の操作 1 つ:
   **AdMob → iOS の「割り勘 wa/ri」→ Firebase にリンク（`wari-39ca1` / `ynetlabo.wari`）
   ＋ ユーザー指標を有効化。** Android は 7/4 にこれをやって翌日から計上が始まっている → §5.1
7. **Flutter の `admob.json` を点検する。** `ios.bannerUnitId` が Android と同じ ID
   （`…/7169159178`）になっている。`wari-android` は Android 向けビルドなので**実害は無い**が、
   iOS の実値は `…/4287190753` なので**死んだ設定として残っている**。直すか消すか決める
8. **iOS の `export_csv_ad_completed` / `export_pdf_ad_completed` が消えている。**
   2026-04-13（v1.20.5）に入り、2026-06-06（v1.28.65）までに削除された。
   GA4 の累計 10 件 / 17 件は全部 iOS で、いま出ている分は**旧ビルドの残存ユーザー**。
   Android にはそもそも実装が無い。エクスポート時のリワード完了を測りたいなら
   **両方に入れ直す**。測らないなら決めて記録する

対象: `wari-ios` / `wari-android/wari_flutter` / `wari-webapp/wari_flutter`（`wari-v2-web` は確認のみ）

### R2 空ルームの主導線を招待にする（9 月 W2）

**決定**: 「これは UX を損ねる可能性があります。むしろオーナーが使うのをやめた可能性が
ありますね？」（2026-08-28、コメント）→ 強制遷移を破棄

`create_room_page.dart` は作成後 `Navigator.of(context).pop(room.roomId)` で戻すだけで、
招待画面へは遷移しない。支出 0 件の空状態を、招待が主アクションのデザインに置き換える。
モーダルと強制遷移は使わない（§3 の 4）。

**原因が未確定でも入れてよい。** 常設の導線は (a)(b) どちらでも害が無い。
ただし**効果の判定は R1 の内訳が取れてから**。効果が出なければ (b) だったという情報になる。

### R3 結果画面に「未招待のメンバー」を常設する（9 月 W3）

**決定**: 「これも登録直後に出すと鬱陶しいと思われそうです」（2026-08-28、コメント）
→ 保存直後のポップアップを破棄

置く場所は `/room/split`（`room_warikan_page.dart`）。この画面を選ぶ理由は 2 つ。

- **既に「満足の瞬間」として扱われている。** `room_warikan_page.dart:78` で
  `ReviewManager.requestReviewIfEligible(ReviewReason.splitResultViewed)` が呼ばれている
- **ここまで来る人には共有意欲がある。** `share_warikan_image` が月 145 回発火している

### R4 レビュー依頼（実装済み・測るだけ）

**決定**: 「いいアイデアです。これは進めたいです。」（2026-08-28、コメント）

開発は不要。`wari-android/wari_flutter` に既に入っている。

- `pubspec.yaml`: `in_app_review: ^2.0.9`
- `review_manager.dart`: `_minTransactionCount = 2`、`_repeatCooldown = 120 日`、
  `enum ReviewReason { splitResultViewed, shareCompleted }`
- 発火点: `room_warikan_page.dart:78`、`editor_room_share_page.dart:115`
- 取引カウント: `editor_tran_page.dart:485, 1265`

やること: **両ストアのレビュー件数と平均評価の現在値を控える**。1 月末に再測する。
増えていなければ発火条件を見直す（緩める前に、まず現在値を取る）。

---

## 7. やらないと決めたこと

あとで「なぜやらなかったか」を探し直さなくて済むように残す。

| やらないこと | 理由 |
|---|---|
| **広告出稿（Apple Search Ads / Google Ads）** | 2026-08-28 に「当面ゼロ円」と決めた。CPI ¥76 は破格に安いが、広告収益が月 2 程度で回収の見込みがゼロ。判断は 2027 年 2 月に持ち越す（下記 §8） |
| **技術記事（Claude / Flutter 系）の増産** | 届くのは開発者で、割り勘・慶弔・シフトのユーザーではない。個人の露出には効くがダウンロードには効かない。実用記事に比重を移す |
| **soroe の集客** | 実利用率 8.3%・確定到達 11.8%。穴を塞ぐのが先。11 月 W11–12 まで着手しない |
| **mapengu / hue の機能追加** | 実利用がほぼ無い。mapengu はペンギン判定カメラという広報素材としてだけ使う（世界ペンギンデー 4/25 が次の機会）。開発リソースは投じない |
| **価値提供の前に出すモーダル・強制遷移** | ルーム作成直後の招待画面への強制遷移、支出保存直後のポップアップ。どちらも「まだ何も起きていない」段階で他人を巻き込む要求になる。常設の導線に置き換える（§3 の 4）|
| **5 アプリ横並びの施策** | いまの最大の非効率。wa/ri に寄せる |
| **wari-app / wa-ri の 2 ホスト問題** | GROWTH.md §6 のとおり、引き続き手を付けない |

---

## 8. 2027 年 2 月に判断すること

ゼロ円の外側の話。1 月末の数字が出てから決める。

0. ~~広告のプラットフォーム別計測を入れるか~~ → **9 月 W1 に前倒し**（設定 1 行）。
   2 月には実数が揃った状態で判断できる
1. **広告を使うか。** 共有率の改修だけで新規が倍近くまで伸びていれば、広告は要らない。
   伸びが頭打ちなら、ASA を忘年会シーズンに集中投下する案が残る（CPI ¥76 実績、
   月 3 万円で 400 install 相当）。**通年で薄く撒くのは効かないので、やるなら 11 月下旬〜1 月に集中**
2. **収益化を設計するか。** いまは広告収益が実質ゼロで、投資の回収経路が無い。
   知名度を買い続けるには、どこかで回収の道を作るか、「買わない」と決め切るかのどちらか
3. **mapengu を広報に使うか。** 4/25 の世界ペンギンデーに向けるなら 3 月には動き出す必要がある
