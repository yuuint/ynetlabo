# 流入と認知を増やすための運用メモ

ynetlabo.net の技術的 SEO（sitemap / canonical / OG / 構造化データ / GA4 / RSS）は
すでに一通り整っている。伸びしろは **検索需要のあるページを持つこと** と
**サイトの外に出ていくこと** の2つ。ここはその運用手順を置く場所。

---

## 1. いま何が入っているか（2026-08-21 時点）

| 追加したもの | URL | 狙い |
|---|---|---|
| ご祝儀・香典の相場早見表 | `/tools/souba` | 「香典 相場」「ご祝儀 相場 友人」等の検索流入 → tsutsum |
| お返し（半返し）計算機 | `/tools/okaeshi` | 「半返し 計算」「香典返し 時期」等の検索流入 → tsutsum |
| ツール一覧 | `/tools` | 上2つのハブ |
| アプリ紹介ページ ×5 | `/apps/<slug>` | 指名検索の受け皿・ストア外の説明場所 |
| アプリ一覧 | `/apps` | 同上 |
| ストア遷移の計測 | GA4 `store_click` | どのページがダウンロードに効いたか |
| ツール利用の計測 | GA4 `tool_used` | ツールが実際に使われているか |
| クロス投稿の原稿 | `crosspost/` | Qiita / Zenn への転載 |
| 香典袋の表書きの記事 | `/archives/222` | 「御霊前 御仏前 違い」「香典袋 書き方」 |
| 内祝い・快気祝いの文例 | `/archives/223` | 「内祝い のし」「快気祝い メッセージ 文例」 |
| シフト希望の集め方 | `/archives/224` | 「シフト 希望 集め方」→ soroe |
| 割り勘の端数と精算 | `/archives/225` | 「割り勘 端数」「割り勘 計算 やり方」→ wa/ri |
| IndexNow | `npm run indexnow` | Bing へ更新を即通知 |
| 画像の WebP 化 | `npm run images` | 記事画像 1,808KB → 153〜355KB |
| sitemap の lastmod | 自動 | 更新日を検索側に伝える（45/58 URL） |
| 記事ごとの OG 画像 | `npm run og:articles` | SNS で記事ごとに違う絵が出る |

相場データは **tsutsum が正**。アプリ側を直したら必ず取り込み直す。

```bash
cd ynetlabo-site && npm run sync:souba
```

---

## 2. Search Console の見かた（登録済み・未活用の状態から）

見るのは月1回、5分でよい。順番はこの3つだけ。

### ① 検索パフォーマンス → クエリ
- 期間を「過去3か月」にする
- **表示回数が多いのにクリック率が低いクエリ**を探す。ここが一番おいしい。
  タイトルと description を書き換えるだけでクリックが増える
- **順位 5〜20位のクエリ**を探す。あと一歩で1ページ目。そのクエリを厚くした
  追記や、専用ページの新設で上がる余地が大きい

### ② ページ
- どのページが表示回数を稼いでいるか。予想と違うページが上位に来ていたら、
  そこが「世間から見たこのサイトの入口」

### ③ インデックス作成 → ページ
- `/tools/souba` `/tools/okaeshi` `/apps/*` が **「インデックスに登録済み」** に
  入るまで数日〜数週間かかる。公開直後は URL 検査から手動でインデックス登録を
  リクエストしておくと早い（1日あたりの上限あり）

> 注意: ドメインプロパティ（`ynetlabo.net`）で登録していれば
> `wa-ri.` `mapengu.` のサブドメインも一緒に見られる。
> URL プレフィックスで登録している場合は、サブドメインが別扱いになるので
> ドメインプロパティを追加したほうがよい（DNS TXT レコード1つで済む）。

## 3. Bing Webmaster Tools（登録済み）

ChatGPT の検索や Copilot は Bing のインデックスを参照するので、ここも見る。

2026-08-20 の SEO 分析で出ていた3件の状態:

| 指摘 | 重大度 | 状態 |
|---|---|---|
| Meta descriptions が短すぎる（10ページ） | 中 | **対応済み**（最短 17字 → 57字） |
| 高品質ドメインからの被リンクが不足 | 中 | 未対応 → §5 |
| IndexNow が採用されていない | 高 | **対応済み** → 下記 |

対応後にもう一度 SEO 分析を回すと、指摘が消えたか確認できる（再スキャンは数日おき）。

### IndexNow（設定済み）

Bing の SEO 分析で「IndexNow が採用されていません」と高重大度で指摘されていたので入れた。
クロールを待たずにインデックスを促せる（Google は非対応、Bing / Yandex などが対応）。

**公開が反映されてから**実行する。push 直後ではなく、1〜2分待ってから。

```bash
cd ynetlabo-site && npm run indexnow          # 直近コミットで変わったページだけ送る
cd ynetlabo-site && npm run indexnow -- --all # 全ページ送る（大きな作り直しのとき）
cd ynetlabo-site && npm run indexnow -- --dry # 送らずに対象を確認する
```

鍵ファイルは `ynetlabo-site/public/452f20d7e5f3335996b14f3d2bcf9891.txt`。
**消すと 403 になる**ので、消さないこと。

---

## 4. クロス投稿の運用

```bash
cd ynetlabo-site && npm run crosspost
```

`crosspost/qiita/` と `crosspost/zenn/` に原稿ができる。詳細は
[crosspost/README.md](crosspost/README.md)。

### 出す順番（反応が大きい順）

| 順 | 記事 | 出し先 |
|---|---|---|
| 1 | 220 Claude のスキルを10個まとめて公開した | Zenn → Qiita |
| 2 | 216 毎セッション読まれる CLAUDE.md をどう書くか | Zenn → Qiita |
| 3 | 215 spec と ADR で Claude の「記憶喪失」をカバーする | Zenn → Qiita |
| 4 | 221 使い方ガイドの画面図は1枚もスクショを撮っていない | Zenn |
| 5 | 219 試験前日に Claude で一夜漬けした | Qiita |
| 6 | 202 AI に UI 実装を任せるなら「テキストだけ」は損をする | Qiita |

- **1日1本まで。** まとめて出すと共倒れになる
- 投稿は火〜木の朝（9時前後）か夜（21時前後）が読まれやすい
- 出したら X でも1本流す。記事のリンクだけでなく、**記事の中で一番強い1文**を
  引用して置くほうが伸びる

---

## 5. 被リンクを増やす

Bing の SEO 分析で「高品質のドメインからのインバウンドリンクが不適切です」と
指摘されている項目。**サイトの外からリンクされていない**、という意味。
コードでは解決できないので、ここは手を動かすしかない。

効く順。

1. **Qiita / Zenn へのクロス投稿**（§4）。記事の冒頭と末尾から原文へリンクが張られる。
   一番手数が少なくて確実
2. **GitHub のプロフィールとリポジトリ**。[claude-skills](https://github.com/yuuint/claude-skills)
   の README から `/skills` へリンクする。GitHub は強いドメイン
3. **ストアの説明文**。App Store / Google Play のサポート URL を
   `/apps/<slug>` に向ける。いまストア側がトップや旧 URL を指しているなら直す
4. **X のプロフィール**とポストから、記事・ツールへ
5. **個人開発系のまとめ記事・アドベントカレンダー**への参加。年末に効く

やってはいけないのは、相互リンクの依頼と有料リンク。
短期的にも効かないうえ、あとで外すのが面倒になる。

## 6. 対象外にしたもの

### wa/ri が2つのホストで公開されている（2026-08-21 時点、対応しないと決めた）

調べたときの状態を残しておく。再発見して調べ直さなくて済むように。

| | wari-app.ynetlabo.net | wa-ri.ynetlabo.net |
|---|---|---|
| HTTP | 200 | 200 |
| IP | 160.251.166.165 | 同じ |
| canonical | 自分自身 | 自分自身 |
| robots | index, follow | index, follow |

同じアプリが別ホストで公開され、どちらも自分を canonical と宣言しているため、
検索側からは別サイト2つに見える。「割り勘アプリ」の評価と被リンクが分散する。
Bing Webmaster には `wari-app.../sitemap.xml`（検出 URL 1件）も登録されたままになっている。

直すなら `wari-app` → `wa-ri` の 301 が本筋（次点は canonical を wa-ri に向ける）。
ただし他リポジトリとサーバー設定の話になるため、**今回は手を付けないと決めた**。
このサイト（ynetlabo.net）側のリンクはすべて `wa-ri` を向いているので、
放置しても本体の評価が下がることはない。

## 7. 次に作るとよいページ

いまのところ白紙で、検索需要があるところ。

1. **香典返しの品物選び**（「香典返し 品物 おすすめ」「香典返し カタログギフト」）。
   ただし物販に寄るとアフィリエイト色が出るので、tsutsum の「お返し候補」機能と
   絡めた実用寄りの内容にしたい
2. **法要の準備**（「四十九日 準備」「一周忌 服装」）。222・223 と同じ読者が続けて読む
3. **旅行の精算**（「旅行 割り勘 アプリ」「海外旅行 割り勘 レート」）。225 の続き、
   外貨ルームの実演になる

いずれも「表や計算を先に見せて、解説は後ろ」の構成にする。
読ませてから使わせるより、使わせてから読ませるほうが滞在も被リンクも取れる。

## 8. 画像を足したとき

記事に新しい画像を置いたら、WebP を作ってからビルドする。

```bash
cd ynetlabo-site && npm run images     # public/images/ の 60KB 超を WebP 化
cd ynetlabo-site && npm run og:articles # ヒーロー画像の無い記事の OG 画像
```

- `Picture.astro` が `<name>-640/960/1280.webp` を srcset に載せる。
  変換していない画像を渡しても、元ファイルにフォールバックするので壊れない
- 生成物は `public/images/` に置かれ、git で追跡する（ビルド環境に cwebp が
  無くても公開できるようにするため）

**内部リンクは末尾スラッシュを付けること**（`/tools/souba/`）。
canonical と sitemap がその形なので、付けないと 301 が挟まる。

## 9. 見る数字

GA4 の探索で、この3つだけ見ればよい。

- `store_click`（`from_path` 別）… どのページがダウンロードを生んだか
- `tool_used`（`tool` 別）… ツールが空振りしていないか
- ランディングページ別のセッション … 入口がトップから記事・ツールに移っていくか

トップページのセッションが増えないことは問題ではない。
**入口が増えて、トップの比率が下がっていくのが正常**。
